import 'package:flutter/material.dart';

class inforProduct extends StatelessWidget {
  const inforProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}